#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int main()
{
    int n1,n2;
    scanf("%d %d",&n1,&n2);
    int arr[n2][2];
    for(int i=0;i<n2;i++)
    {
        scanf("%d %d",&arr[i][0],&arr[i][1]);
    }
    for (int i = 0; i < n2; i++)
    {
        if (i)
        {
            /* code */
        }
        
    }
    
    return 0;
}